<div class="container">
    <% if (title) { %>
        <h1>
            <%= title %>
        </h1>
        <% } %>
            <div id="link-container">
                <ul>
                    <li><a href="/account/update/<%= accountData.account_id %>">Update Account
                            Information</a></li>
                    <li><a href="/inventory">Go to Inventory Management</a></li>
                </ul>
            </div>
            <% if (messages().length) { %>
                <div class="flash-message"><%- messages() %></div>
                <% } %>

                    <% if (errors && errors.array().length> 0) { %>
                        <ul class="notice">
                            <% errors.array().forEach(error=> { %>
                                <li>
                                    <%= error.msg %>
                                </li>
                                <% }) %>
                        </ul>
                        <% } %>

                            <h2>Welcome, <%= accountData.account_firstname %>
                            </h2>
                            <p>You’re logged in.</p>
</div>